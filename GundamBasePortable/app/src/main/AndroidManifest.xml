<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/custom_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/custom_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.GundamBase.Splash">

        <!-- Splash / Launch Activity -->
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:noHistory="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity (Hangar + System Config + Haro's Guide tabs) -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateHidden" />

        <!-- Detail Activity (Suit Specs) -->
        <activity
            android:name=".ui.SuitDetailActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".ui.MainActivity" />

    </application>

</manifest>
